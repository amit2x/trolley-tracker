#ifndef SECRETS_H
#define SECRETS_H

// =====================================================
// secrets.h.example — template for secrets.h
// =====================================================
// Copy this file to secrets.h and replace the placeholder
// values below with your real WiFi and MQTT broker settings.
// secrets.h itself is git-ignored and must never be committed.
// =====================================================

const char* WIFI_SSID   = "YOUR_WIFI_SSID";
const char* WIFI_PASS   = "YOUR_WIFI_PASSWORD";
const char* MQTT_BROKER = "192.168.1.100";   // IP address of the machine running Mosquitto

#endif
